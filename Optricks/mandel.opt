/*
def char den(int d):
	if    d > 999	return '*';
	elif  d > 200	return '+';
	elif  d > 020	return '.';
	else         	return ' ';

def int mand(complex z0){
	auto z = z0
	for iter in range(1000):
		if( z.mag2() > 4 ) return iter
		else z = z**6 + z0
	return 1000
}

File out = File('happy.txt','w');
for im in range(-1.25,1.25,.04){
	for re in range(-2,1,.02):
		out << den(mand(complex(re,im)))
	out << '\n';
}
out.close();
*/
auto tmp = (extern char _Z6myTestc(char c));
print(tmp('d'));
